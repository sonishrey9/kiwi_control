# Tester Packet

Goal: `{{goal}}`

Project: `{{projectName}}`
Profile: `{{profileName}}`

Verify:

- the work followed repo authority and promoted canonical docs before changing shared behavior
- config loads
- generated overlays validate
- managed markers are stable
- repo-local outputs are portable
- routing resolves to an explainable decision
- packets are coherent for the selected profile
- continuity artifacts are consulted for non-trivial resumed work
- guarded work would trigger push-check before release or push advice

Prefer the smallest relevant checks first, then broaden only if shared behavior changed.
