{
  "name": "shrey-junior-workspace",
  "version": "0.2.0-beta.1",
  "private": true,
  "type": "module",
  "description": "Repo-first local control plane for coding agents",
  "workspaces": [
    "packages/sj-core",
    "packages/sj-cli",
    "apps/sj-ui"
  ],
  "scripts": {
    "build": "npm run build -w @shrey-junior/sj-core && npm run build -w shrey-junior && npm run build -w @shrey-junior/sj-ui",
    "test": "npm run test -w shrey-junior",
    "audit": "npm run build && node packages/sj-cli/dist/cli.js audit",
    "check": "npm run build && node packages/sj-cli/dist/cli.js check",
    "smoke": "npm run build && bash scripts/smoke-test.sh",
    "release:manifest": "node scripts/prepare-release-manifest.mjs",
    "release:check": "npm run build && npm test && bash scripts/smoke-test.sh && npm run release:manifest",
    "ui:dev": "npm run tauri:dev -w @shrey-junior/sj-ui",
    "ui:build": "npm run build -w @shrey-junior/sj-ui"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  }
}
