# Bugfix Flow

1. `run "fix the auth callback bug" --profile product-build`
2. Use the Codex packet as the primary implementation packet.
3. Use the Claude packet for planning or docs clarification if needed.
4. Use the reviewer or tester packets when the route requires them.
