# Strict Production Profile

Use `strict-production` when:

- the repo is shared and business-critical
- high-risk changes need planner and reviewer coverage
- `sync --backup` should be the default behavior
