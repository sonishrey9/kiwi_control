# Tool-Aware Copilot Flow

1. Read repo authority and `.agent/checks.yaml`.
2. Use existing packets when they are present.
3. Stay direct only for trivial edits.
4. For multi-file, guarded, or contract-sensitive work, assume `run`, `fanout`, or `dispatch` framing should exist first.
