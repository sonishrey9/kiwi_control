{
  "name": "shrey-junior",
  "version": "0.2.0-beta.1",
  "type": "module",
  "description": "Installable CLI for the Kiwi Control repo-first control plane",
  "bin": {
    "kiwi-control": "dist/cli.js",
    "kc": "dist/cli.js",
    "shrey-junior": "dist/cli.js",
    "sj": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b --force && node ../../scripts/remove-macos-metadata.mjs dist",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "smoke": "npm run build && bash ../../scripts/smoke-test.sh"
  },
  "dependencies": {
    "@shrey-junior/sj-core": "0.2.0-beta.1"
  }
}
